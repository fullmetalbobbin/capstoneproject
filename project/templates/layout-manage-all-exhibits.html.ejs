<!DOCTYPE html>
<html lang="en">

    <head>
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>Manage: Exhibits</title>
        <style>
            ul {list-style-type: none;}
        </style>
        <link href="site.css" rel="stylesheet" type="text/css" />
        <link rel="icon" href="../assets/buttons/icons8-button7-64.png" type="image/png" />
    </head>

    <body>

        <section id="page">       
            <nav><%- navi %></nav>
            <header>Manage Exhibits</header>
            <main>
                
                List of exhibits to manage -redirects to single exhibit

                <table class="table">

                    <thread>
                        <tr>
                            <th scope="col">ExhibitID</th>
                            <th scope="col">ExhibitName</th>
                            <th scope="col">ExhibitQR</th>
                            <th scope="col">ExhibitPhoto</th>
                        </tr>
                    </thread>
                    <tbody>
                        <% exhibits.forEach((exhibit) => { %>
                        <tr>
                            <th scope="row"><% exhibit.ExhibitID %></th>
                            <td><%= exhibit.ExhibitID %></td>
                            <td><%= exhibit.ExhibitName %></td>
                            <td><img src="<%= exhibit.PathToQRAsset %>" type="image/png" alt="QR Code"></td>
                            <td><img src="<%= exhibit.PathToPhotoAsset %>" type="image/png" alt="Photo"></td>
                            <td><a href="/exhibit/<%= exhibit.id %>"><button class="btn" type="submit">Edit Exhibit</button></a></td>
                        </tr>
                        <% }); %>
                    </tbody>
                </table>
            
            </main>
        </section>

        <link rel="stylesheet" href="site.css" type="text/css"/>
        <script src="site.js"></script>

    </body>
</html>

