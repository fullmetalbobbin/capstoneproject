<!DOCTYPE html>
<html lang="en">

    <head>
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>Manage: Users</title>
        <style>
            ul {list-style-type: none;}
        </style>
        <link href="site.css" rel="stylesheet" type="text/css" />
        <link rel="icon" href="../assets/buttons/icons8-button7-64.png" type="image/png" />
    </head>

    <body>

        <section id="page">       
            <nav><%- navi %></nav>
            <header>Manage Users</header>
            <main>
                List of users - redirects to single user?
                forach needed here  - will be the link to users/list?

                <table class="table">

                    <thead>
                      <tr>
                        <th scope="col">UserID</th>
                        <th scope="col">UserHandle</th>
                        <th scope="col">UserFirstName</th>
                        <th scope="col">UserLastName</th>
                        <th scope="col">UserEmail</th>
                        <th scope="col">UserRole</th>
                      </tr>
                    </thead>
                    <tbody>
                    <% users.forEach((user) => { %>
                      <tr>
                        <th scope="row"><%= user.id %></th>
                        <td><%= user.handle %></td>
                        <td><%= user.firstName %></td>
                        <td><%= user.lastName %></td>
                        <td><%= user.email %></td>
                        <td><%= user.role %></td>
                        <td><a href="/users/<%= user.id %>"><button class="btn" type="submit">Edit User</button></a></td>
                      </tr>
                    <% }); %>
                    </tbody>                 
                 </table>

            </main>
        </section>
        
        <link rel="stylesheet" href="site.css" type="text/css"/>
        <script src="site.js"></script>

    </body>
</html>
