<!DOCTYPE html>
<html lang="en">

    <head>
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>Manage: Users</title>
        <style>
            ul {list-style-type: none;}
        </style>
        <link href="site.css" rel="stylesheet" type="text/css" />
        <link rel="icon" href="../assets/buttons/icons8-button7-64.png" type="image/png" />
    </head>

    <body>

        <section id="page">       
            <nav><%- navi %></nav>
            <header>Manage Users</header>
            <main>
                List of users - redirects to single user?
                forach needed here  - will be the link to users/list?
                <div class="main-container" id="user-list-container">
                <table class="table">

                    <thead>
                      <tr>
                        <th scope="col">UserID</th>
                        <th scope="col">UserHandle</th>
                        <th scope="col">FirstName</th>
                        <th scope="col">LastName</th>
                        <th scope="col">Email</th>
                        <th scope="col">Role</th>
                      </tr>
                    </thead>
                    <tbody>
                    <% users.forEach((user) => { %>
                      <tr>
                        <th scope="row"><%= user.UserID %></th>
                        <td><%= user.UserHandle %></td>
                        <td><%= user.UserFirstName %></td>
                        <td><%= user.UserLastName %></td>
                        <td><%= user.UserEmail %></td>
                        <td><%= user.UserRole %></td>
                        <td><a href="/users/<%= user.UserID %>"><button class="btn" type="submit">EditUser</button></a></td>
                      </tr>
                    <% }); %>
                    </tbody>                 
                </table>
                </div>
            </main>
        </section>
        
        <link rel="stylesheet" href="site.css" type="text/css"/>
        <script src="site.js"></script>

    </body>
</html>
